# Updated 2022-11-20 based on:
#  IP2Location (docs)           https://www.ip2location.com/database/ip2location
#  IP2Proxy    (docs)           https://www.ip2location.com/database/ip2proxy
#  IP2Location (fields, docs)   https://www.ip2location.com/database/db25-ip-country-region-city-latitude-longitude-zipcode-timezone-isp-domain-netspeed-areacode-weather-mobile-elevation-usagetype-addresstype-category
#  IP2Proxy    (fields, docs)   https://www.ip2location.com/database/px11-ip-proxytype-country-region-city-isp-domain-usagetype-asn-lastseen-threat-residential-provider
#  IP2Location (offsets, types) https://github.com/ip2location/ip2location-go/blob/v9.5.0/ip2location.go
#  IP2Proxy    (offsets, types) https://github.com/ip2location/ip2proxy-go/blob/v3.4.1/ip2proxy.go

+AddressType
  @ address_type
  | IP address types as defined in Internet Protocol version 4 (IPv4) and
  | Internet Protocol version 6 (IPv6).
  |   - (A) Anycast - One to the closest
  |   - (U) Unicast - One to one
  |   - (M) Multicast - One to multiple
  |   - (B) Broadcast - One to all

+AreaCode
  @ area_code
  | A varying length number assigned to geographic areas for call between
  | cities.
  |
  | See https://www.ip2location.com/area-code-coverage.

+AS
  @ as
  | Autonomous system number (ASN).

+ASN
  @ asn
  | Autonomous system (AS) name.

+Category
  @ category
  | The domain category is based on IAB Tech Lab Content Taxonomy.
  |
  | These categories are comprised of Tier-1 and Tier-2 (if available) level
  | categories widely used in services like advertising, Internet security and
  | filtering appliances.
  |
  | See https://www.ip2location.com/free/iab-categories.

+City
  @ city
  | City name.

+CountryCode
  @ country_code
  | Two-character country code based on ISO 3166.

+CountryName
  @ country_name
  | Country name based on ISO 3166.

+Domain
  @ domain
  | Internet domain name associated with IP address range.

+Elevation
  @ elevation
  | Average height of city above sea level in meters (m).

+IDDCode
  @ idd_code
  | The IDD prefix to call the city from another country.

+ISP
  @ isp
  | Internet Service Provider or company's name.

+LastSeen
  @ last_seen
  | Proxy last seen in days.

+Latitude
  @ latitude
  | City latitude. Defaults to capital city latitude if city is unknown.

+Longitude
  @ longitude
  | City longitude. Defaults to capital city longitude if city is unknown.

+MCC
  @ mcc
  | Mobile Country Codes (MCC) as defined in ITU E.212 for use in identifying
  | mobile stations in wireless telephone networks, particularly GSM and UMTS
  | networks.

+MNC
  @ mnc
  | Mobile Network Code (MNC) is used in combination with a Mobile Country Code
  | (MCC) to uniquely identify a mobile phone operator or carrier.

+MobileBrand
  @ mobile_brand
  | Commercial brand associated with the mobile carrier.
  |
  | See https://www.ip2location.com/mobile-carrier-coverage.

+NetSpeed
  @ net_speed
  | Internet Connection Type
  |   - (DIAL) dial up
  |   - (DSL) broadband/cable/fiber/mobile
  |   - (COMP) company/T1

+Provider
  @ provider
  | Name of VPN provider if available.

+ProxyType
  @ proxy_type
  | Type of proxy.
  |   - (VPN) Anonymizing VPN services. These services offer users a publicly
  |     accessible VPN for the purpose of hiding their IP address. Anonymity:
  |     High.
  |   - (TOR) Tor Exit Nodes. The Tor Project is an open network used by those
  |     who wish to maintain anonymity. Anonymity: High.
  |   - (DCH) Hosting Provider, Data Center or Content Delivery Network. Since
  |     hosting providers and data centers can serve to provide anonymity, the
  |     Anonymous IP database flags IP addresses associated with them.
  |     Anonymity: Low.
  |   - (PUB) Public Proxies. These are services which make connection requests
  |     on a user's behalf. Proxy server software can be configured by the
  |     administrator to listen on some specified port. These differ from VPNs
  |     in that the proxies usually have limited functions compare to VPNs.
  |     Anonymity: High.
  |   - (WEB) Web Proxies. These are web services which make web requests on a
  |     user's behalf. These differ from VPNs or Public Proxies in that they are
  |     simple web-based proxies rather than operating at the IP address and
  |     other ports level. Anonymity: High.
  |   - (SES) Search Engine Robots. These are services which perform crawling or
  |     scraping to a website, such as, the search engine spider or bots engine.
  |     Anonymity: Low.
  |   - (RES) Residential proxies. These services offer users proxy connections
  |     through residential ISP with or without consents of peers to share their
  |     idle resources. Only available with PX10 & PX11. Anonymity: Medium.

+Region
  @ region
  | Region or state name.

+Threat
  @ threat
  | Security threat reported.
  |   - (SPAM) Email and forum spammers
  |   - (SCANNER) Network security scanners
  |   - (BOTNET) Malware infected devices

+Timezone
  @ time_zone
  | UTC time zone (with DST supported).

+UsageType
  @ usage_type
  | Usage type classification of ISP or company.
  |   - (COM) Commercial
  |   - (ORG) Organization
  |   - (GOV) Government
  |   - (MIL) Military
  |   - (EDU) University/College/School
  |   - (LIB) Library
  |   - (CDN) Content Delivery Network
  |   - (ISP) Fixed Line ISP
  |   - (MOB) Mobile ISP
  |   - (DCH) Data Center/Web Hosting/Transit
  |   - (SES) Search Engine Spider
  |   - (RSV) Reserved

+WeatherStationCode
  @ weather_station_code
  | The special code to identify the nearest weather observation station.

+WeatherStationName
  @ weather_station_name
  | The name of the nearest weather observation station.

+Zipcode
  @ zip_code
  | ZIP code or Postal code.
  |
  | See https://www.ip2location.com/zip-code-coverage.

%1 IP2Location DB
  | IP2Location™ IP Address Geolocation Database provides a solution to deduce
  | the geolocation of a device connected to the Internet and to determine the
  | approximate geographic location of an IP address along with some other
  | useful information like country, region or state, city, latitude and
  | longitude, ZIP/Postal code, time zone, Internet Service Provider (ISP) or
  | company name, domain name, net speed, area code, weather station code,
  | weather station name, mobile country code (MCC), mobile network code (MNC)
  | and carrier brand, elevation, usage type, address type and advertising
  | category.

  1 str * country_code
    str 3 country_name

  2 str * country_code
    str 3 country_name
    str * isp

  3 str * country_code
    str 3 country_name
    str * region
    str * city

  4 str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp

  5 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude

  6 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * isp

  7 str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain

  8 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * isp
    str * domain

  9 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code

 10 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * isp
    str * domain

 11 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone

 12 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain

 13 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * time_zone
    str * net_speed

 14 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed

 15 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * idd_code
    str * area_code

 16 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code

 17 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * time_zone
    str * net_speed
    str * weather_station_code
    str * weather_station_name

 18 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code
    str * weather_station_code
    str * weather_station_name

 19 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * isp
    str * domain
    str * mcc
    str * mnc
    str * mobile_brand

 20 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code
    str * weather_station_code
    str * weather_station_name
    str * mcc
    str * mnc
    str * mobile_brand

 21 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * idd_code
    str * area_code
    str * elevation

 22 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code
    str * weather_station_code
    str * weather_station_name
    str * mcc
    str * mnc
    str * mobile_brand
    str * elevation

 23 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * isp
    str * domain
    str * mcc
    str * mnc
    str * mobile_brand
    str * usage_type

 24 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code
    str * weather_station_code
    str * weather_station_name
    str * mcc
    str * mnc
    str * mobile_brand
    str * elevation
    str * usage_type

 25 str * country_code
    str 3 country_name
    str * region
    str * city
    f32 = latitude
    f32 = longitude
    str * zip_code
    str * time_zone
    str * isp
    str * domain
    str * net_speed
    str * idd_code
    str * area_code
    str * weather_station_code
    str * weather_station_name
    str * mcc
    str * mnc
    str * mobile_brand
    str * elevation
    str * usage_type
    str * address_type
    str * category

%2 IP2Proxy PX
  | IP2Proxy™ Proxy Detection Database contains IP addresses which are used as
  | VPN anonymizer, open proxies, web proxies and Tor exits, data center, web
  | hosting (DCH) range, search engine robots (SES) and residential proxies
  | (RES).

  1 str * country_code
    str 3 country_name

  2 str * proxy_type
    str * country_code
    str 3 country_name

  3 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city

  4 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp

  5 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain

  6 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type

  7 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type
    str * asn
    str * as

  8 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type
    str * asn
    str * as
    str * last_seen

  9 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type
    str * asn
    str * as
    str * last_seen
    str * threat

 10 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type
    str * asn
    str * as
    str * last_seen
    str * threat

 11 str * proxy_type
    str * country_code
    str 3 country_name
    str * region
    str * city
    str * isp
    str * domain
    str * usage_type
    str * asn
    str * as
    str * last_seen
    str * threat
    str * provider

